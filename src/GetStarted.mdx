import { Meta } from '@storybook/blocks';

<Meta title="Welcome/Get Started" />

<style>{`
  @import url('https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200');

  .citruss-getstarted-container {
    color: var(--citruss-text-main) !important;
    font-family: "Outfit", sans-serif !important;
    padding: 60px 40px !important;
    background-color: #000000 !important;
    background-image: 
        radial-gradient(at 47% 33%, hsl(162.00, 77%, 40%) 0, transparent 59%), 
        radial-gradient(at 82% 65%, hsl(218.95, 81%, 47%) 0, transparent 55%) !important;
    border-radius: 32px !important;
    border: 1px solid var(--citruss-glass-border) !important;
    box-shadow: 0 30px 60px var(--citruss-glass-shadow) !important;
    position: relative;
    overflow: hidden;
  }

  /* Material Symbols Font Rule Support */
  .material-symbols-rounded {
    font-family: 'Material Symbols Rounded' !important;
    font-weight: normal;
    font-style: normal;
    font-size: 24px;
    line-height: 1;
    letter-spacing: normal;
    text-transform: none;
    display: inline-block;
    white-space: nowrap;
    word-wrap: normal;
    direction: ltr;
    -webkit-font-feature-settings: 'liga';
    -webkit-font-smoothing: antialiased;
  }

  /* Premium Background Blobs */
  .citruss-ambient-blob-1 {
    position: absolute;
    top: -100px;
    right: -100px;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255, 107, 0, 0.15) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
    filter: blur(40px);
    animation: pulseBlob 12s infinite alternate ease-in-out;
  }

  .citruss-ambient-blob-2 {
    position: absolute;
    bottom: -150px;
    left: -100px;
    width: 450px;
    height: 450px;
    background: radial-gradient(circle, rgba(163, 230, 53, 0.1) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
    filter: blur(40px);
    animation: pulseBlob 16s infinite alternate-reverse ease-in-out;
  }

  @keyframes pulseBlob {
    0% { transform: scale(1) translate(0, 0); }
    100% { transform: scale(1.15) translate(30px, -20px); }
  }

  /* Glass Cards */
  .citruss-hero-card {
    position: relative;
    z-index: 1;
    background: var(--citruss-glass-bg);
    backdrop-filter: blur(var(--citruss-glass-blur));
    border: 1px solid var(--citruss-glass-border);
    box-shadow: 0 8px 32px var(--citruss-glass-shadow);
    border-radius: 24px;
    padding: 36px;
    margin-bottom: 48px;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.4s ease;
  }

  .citruss-hero-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 48px rgba(255, 107, 0, 0.12);
  }

  .citruss-grid-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
    margin-bottom: 48px;
    position: relative;
    z-index: 1;
  }

  .citruss-feature-card {
    background: var(--citruss-glass-bg);
    backdrop-filter: blur(var(--citruss-glass-blur));
    border: 1px solid var(--citruss-glass-border);
    border-radius: 20px;
    padding: 28px;
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .citruss-feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .citruss-feature-card.card-orange::before {
    background: linear-gradient(to right, var(--citruss-orange), #ff9f43);
  }

  .citruss-feature-card.card-lime::before {
    background: linear-gradient(to right, var(--citruss-lime), #84cc16);
  }

  .citruss-feature-card.card-info::before {
    background: linear-gradient(to right, var(--citruss-info), #06b6d4);
  }

  .citruss-feature-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 12px 30px var(--citruss-glass-shadow);
    border-color: rgba(255, 107, 0, 0.3);
  }

  .citruss-feature-card:hover::before {
    opacity: 1;
  }

  .citruss-feature-card h3 {
    margin: 0 0 12px 0 !important;
    font-size: 1.25rem !important;
    font-weight: 700 !important;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .citruss-feature-card p {
    margin: 0 !important;
    font-size: 0.95rem !important;
    line-height: 1.6 !important;
    color: var(--citruss-text-muted) !important;
  }

  /* Interactive Live Sandbox Showcase */
  .citruss-sandbox-wrapper {
    background: rgba(15, 23, 42, 0.3);
    border: 1px solid var(--citruss-glass-border);
    border-radius: 24px;
    padding: 32px;
    margin-bottom: 48px;
    position: relative;
    z-index: 1;
  }

  /* Terminal Code Styling */
  .citruss-terminal-wrapper {
    background: #0b0f19 !important;
    border: 1px solid rgba(255, 255, 255, 0.05) !important;
    border-radius: 16px !important;
    padding: 20px !important;
    position: relative;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0,0,0,0.4) !important;
  }

  .citruss-terminal-header {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 12px;
    padding-bottom: 8px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .citruss-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .citruss-dot-red { background: #ef4444; }
  .citruss-dot-yellow { background: #f59e0b; }
  .citruss-dot-green { background: #10b981; }

  .citruss-terminal-title {
    margin-left: 6px;
    font-size: 0.8rem;
    color: #4b5563;
    font-family: 'Outfit', sans-serif;
    font-weight: 500;
  }

  .citruss-getstarted-container pre {
    background: transparent !important;
    border: none !important;
    padding: 0 !important;
    margin: 0 !important;
    font-family: 'Fira Code', monospace !important;
  }

  .citruss-getstarted-container code {
    font-family: 'Fira Code', monospace !important;
    font-size: 0.9rem !important;
  }

  .code-keyword { color: #f43f5e; }
  .code-string { color: #10b981; }
  .code-comment { color: #6b7280; font-style: italic; }
  .code-function { color: #3b82f6; }
  .code-variable { color: #a855f7; }

  /* Premium Buttons */
  .citruss-interactive-btn {
    background: linear-gradient(135deg, var(--citruss-orange) 0%, #ff8a00 100%);
    color: #fff !important;
    border: none;
    padding: 12px 24px;
    border-radius: 12px;
    font-family: 'Outfit', sans-serif;
    font-weight: 700;
    font-size: 0.95rem;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(255, 107, 0, 0.3);
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .citruss-interactive-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(255, 107, 0, 0.45);
  }

  .citruss-interactive-btn:active {
    transform: translateY(0);
  }
`}</style>

<div className="citruss-getstarted-container">
  
  {/* Dynamic Ambient Blur Objects */}
  <div className="citruss-ambient-blob-1" />
  <div className="citruss-ambient-blob-2" />

  {/* Hero Header Card */}
  <div className="citruss-hero-card">
    <div style={{ display: 'flex', alignItems: 'center', gap: '28px', flexWrap: 'wrap' }}>
      <div style={{
        background: 'rgba(255, 107, 0, 0.1)',
        border: '1px solid rgba(255, 107, 0, 0.2)',
        borderRadius: '24px',
        padding: '16px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        boxShadow: '0 8px 24px rgba(255, 107, 0, 0.08)'
      }}>
        <img src="./logos/citruss_logo_transparent.png" alt="CitruSS Logo" style={{ height: '76px', width: 'auto', objectFit: 'contain' }} />
      </div>
      <div style={{ flex: '1', minWidth: '280px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '12px', flexWrap: 'wrap' }}>
          <h1 style={{
            fontSize: '3.2rem',
            fontWeight: '900',
            margin: '0',
            background: 'linear-gradient(135deg, #ff6b00 0%, #ffa800 50%, #a3e635 100%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent',
            letterSpacing: '-1.5px',
            lineHeight: '1.1'
          }}>
            CitruSS UI
          </h1>
          <span style={{
            background: 'linear-gradient(135deg, rgba(255, 107, 0, 0.15) 0%, rgba(163, 230, 53, 0.15) 100%)',
            border: '1px solid rgba(255, 107, 0, 0.2)',
            borderRadius: '20px',
            padding: '4px 12px',
            fontSize: '0.85rem',
            fontWeight: '700',
            color: 'var(--citruss-orange)',
            letterSpacing: '0.5px'
          }}>v1.0.0 Stable</span>
        </div>
        <p style={{ fontSize: '1.25rem', color: 'var(--citruss-text-muted)', margin: '12px 0 0 0', fontWeight: '400', lineHeight: '1.5' }}>
          The modern, high-fidelity, and ultra-performant Frosted Glassmorphism UI ecosystem for web applications.
        </p>
      </div>
    </div>
  </div>

  {/* Philosophy / Highlights */}
  <h2 style={{ fontSize: '1.8rem', fontWeight: '800', color: 'var(--citruss-text-main)', marginBottom: '24px', letterSpacing: '-0.5px', display: 'flex', alignItems: 'center', gap: '10px' }}>
    <span className="citruss-icon icon-rounded" style={{ color: 'var(--citruss-orange)', fontSize: '2.2rem' }}>auto_awesome</span> Key Core Features
  </h2>

  <div className="citruss-grid-cards">
    
    <div className="citruss-feature-card card-orange">
      <h3 style={{ color: 'var(--citruss-orange)' }}>
        <span className="citruss-icon icon-rounded">blur_on</span> Premium Glassmorphism
      </h3>
      <p>
        A premium visual layer featuring high-fidelity radial and mesh gradients, sleek glass-blur overlays, and harmonious light/dark-mode aesthetics.
      </p>
    </div>

    <div className="citruss-feature-card card-lime">
      <h3 style={{ color: 'var(--citruss-lime)' }}>
        <span className="citruss-icon icon-rounded">offline_bolt</span> Zero Dependencies
      </h3>
      <p>
        Engineered with absolute zero third-party dependencies. Seamlessly drop CitruSS into vanilla JS/HTML files or robust web frameworks.
      </p>
    </div>

    <div className="citruss-feature-card card-info">
      <h3 style={{ color: 'var(--citruss-info)' }}>
        <span className="citruss-icon icon-rounded">speed</span> Buttery-Smooth (60fps)
      </h3>
      <p>
        Highly-optimized CSS layout rules and hardware-accelerated transforms keep transitions and animations fluid, even on low-end screens.
      </p>
    </div>

  </div>

  {/* Sandbox Preview Showcase */}
  <h2 style={{ fontSize: '1.8rem', fontWeight: '800', color: 'var(--citruss-text-main)', marginBottom: '24px', letterSpacing: '-0.5px', display: 'flex', alignItems: 'center', gap: '10px' }}>
    <span className="citruss-icon icon-rounded" style={{ color: 'var(--citruss-lime)', fontSize: '2.2rem' }}>play_circle</span> Try CitruSS Live Sandbox
  </h2>

  <div className="citruss-sandbox-wrapper">
    <div style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
      <div style={{
        background: 'var(--citruss-glass-bg)',
        border: '1px solid var(--citruss-glass-border)',
        borderRadius: '20px',
        padding: '28px',
        boxShadow: '0 10px 30px var(--citruss-glass-shadow)',
        position: 'relative'
      }}>
        <h3 style={{ margin: '0 0 8px 0', fontSize: '1.3rem', fontWeight: '800', color: 'var(--citruss-orange)', display: 'flex', alignItems: 'center', gap: '8px' }}>
          <span className="citruss-icon icon-rounded">campaign</span> Interactive Preview Card
        </h3>
        <p style={{ margin: '0 0 20px 0', fontSize: '0.95rem', color: 'var(--citruss-text-muted)' }}>
          Test the interactive CitruSS JavaScript engine directly inside Storybook. Tap the button below to dispatch a fully-integrated glassmorphic toast notification.
        </p>
        <button 
          className="citruss-interactive-btn"
          onClick={() => {
            if (window.CitruSS && typeof window.CitruSS.toast === 'function') {
              window.CitruSS.toast({
                title: 'Welcome to CitruSS UI!',
                message: 'CitruSS JS Engine is active and running beautifully in this workspace.',
                type: 'success',
                duration: 4000
              });
            } else {
              alert('CitruSS engine is initializing or not exposed globally. Ready to run!');
            }
          }}
        >
          <span className="citruss-icon icon-rounded">celebration</span> Trigger Live Toast!
        </button>
      </div>
    </div>
  </div>

  {/* Quick Start & Installation */}
  <h2 style={{ fontSize: '1.8rem', fontWeight: '800', color: 'var(--citruss-text-main)', marginBottom: '24px', letterSpacing: '-0.5px', display: 'flex', alignItems: 'center', gap: '10px' }}>
    <span className="citruss-icon icon-rounded" style={{ color: 'var(--citruss-info)', fontSize: '2.2rem' }}>terminal</span> Quick Start & Installation
  </h2>

  <div style={{ display: 'flex', flexDirection: 'column', gap: '24px', position: 'relative', zIndex: '1' }}>
    
    <div>
      <h4 style={{ margin: '0 0 10px 0', fontSize: '1.1rem', fontWeight: '600', color: 'var(--citruss-text-main)', display: 'flex', alignItems: 'center', gap: '6px' }}>
        <span className="citruss-icon icon-rounded" style={{ fontSize: '1.3rem', color: 'var(--citruss-orange)' }}>download</span> 1. Install via Package Manager
      </h4>
      <div className="citruss-terminal-wrapper">
        <div className="citruss-terminal-header">
          <div className="citruss-dot citruss-dot-red" />
          <div className="citruss-dot citruss-dot-yellow" />
          <div className="citruss-dot citruss-dot-green" />
          <span className="citruss-terminal-title">Terminal</span>
        </div>
        <pre>
          <code>
            <span className="code-keyword">npm</span> <span className="code-string">install citruss-ui</span>
          </code>
        </pre>
      </div>
    </div>

    <div>
      <h4 style={{ margin: '0 0 10px 0', fontSize: '1.1rem', fontWeight: '600', color: 'var(--citruss-text-main)', display: 'flex', alignItems: 'center', gap: '6px' }}>
        <span className="citruss-icon icon-rounded" style={{ fontSize: '1.3rem', color: 'var(--citruss-lime)' }}>javascript</span> 2. Include Style and Modules
      </h4>
      <div className="citruss-terminal-wrapper">
        <div className="citruss-terminal-header">
          <div className="citruss-dot citruss-dot-red" />
          <div className="citruss-dot citruss-dot-yellow" />
          <div className="citruss-dot citruss-dot-green" />
          <span className="citruss-terminal-title">app.js</span>
        </div>
        <pre>
          <code>
            <span className="code-comment">// 1. Import the premium CSS stylesheet</span><br />
            <span className="code-string">import 'citruss-ui/dist/citruss.css'</span>;<br /><br />
            <span className="code-comment">// 2. Import the main JavaScript component engine</span><br />
            <span className="code-keyword">import</span> <span className="code-variable">CitruSS</span> <span className="code-keyword">from</span> <span className="code-string">'citruss-ui'</span>;
          </code>
        </pre>
      </div>
    </div>

    <div>
      <h4 style={{ margin: '0 0 10px 0', fontSize: '1.1rem', fontWeight: '600', color: 'var(--citruss-text-main)', display: 'flex', alignItems: 'center', gap: '6px' }}>
        <span className="citruss-icon icon-rounded" style={{ fontSize: '1.3rem', color: 'var(--citruss-info)' }}>html</span> 3. Basic HTML Boilerplate
      </h4>
      <div className="citruss-terminal-wrapper">
        <div className="citruss-terminal-header">
          <div className="citruss-dot citruss-dot-red" />
          <div className="citruss-dot citruss-dot-yellow" />
          <div className="citruss-dot citruss-dot-green" />
          <span className="citruss-terminal-title">index.html</span>
        </div>
        <pre style={{ maxHeight: '280px', overflowY: 'auto' }}>
          <code>
            &lt;<span className="code-keyword">div</span> <span className="code-variable">class</span>=<span className="code-string">"citruss-card"</span>&gt;<br />
            &nbsp;&nbsp;&lt;<span className="code-keyword">div</span> <span className="code-variable">class</span>=<span className="code-string">"card-content"</span>&gt;<br />
            &nbsp;&nbsp;&nbsp;&nbsp;&lt;<span className="code-keyword">h3</span> <span className="code-variable">style</span>=<span className="code-string">"color: var(--citruss-orange);"</span>&gt;CitruSS Active!&lt;/<span className="code-keyword">h3</span>&gt;<br />
            &nbsp;&nbsp;&nbsp;&nbsp;&lt;<span className="code-keyword">p</span>&gt;Translucent frosted glass design engine ready to go.&lt;/<span className="code-keyword">p</span>&gt;<br />
            &nbsp;&nbsp;&nbsp;&nbsp;&lt;<span className="code-keyword">button</span> <span className="code-variable">class</span>=<span className="code-string">"citruss-btn btn-primary"</span> <span className="code-variable">id</span>=<span className="code-string">"start-btn"</span>&gt;Explore&lt;/<span className="code-keyword">button</span>&gt;<br />
            &nbsp;&nbsp;&lt;/<span className="code-keyword">div</span>&gt;<br />
            &lt;/<span className="code-keyword">div</span>&gt;
          </code>
        </pre>
      </div>
    </div>

  </div>

  {/* Tip banner */}
  <div style={{
    marginTop: '48px',
    background: 'var(--citruss-orange-glow)',
    border: '1px solid var(--citruss-glass-border)',
    borderRadius: '20px',
    padding: '24px',
    display: 'flex',
    alignItems: 'center',
    gap: '20px',
    position: 'relative',
    zIndex: '1'
  }}>
    <div style={{
      background: 'rgba(255, 107, 0, 0.1)',
      borderRadius: '50%',
      padding: '10px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'var(--citruss-orange)'
    }}>
      <span className="citruss-icon icon-rounded" style={{ fontSize: '2rem' }}>lightbulb</span>
    </div>
    <div style={{ flex: '1' }}>
      <p style={{ margin: '0', fontSize: '1rem', color: 'var(--citruss-text-main)', fontWeight: '600', marginBottom: '4px' }}>Pro Tip:</p>
      <p style={{ margin: '0', fontSize: '0.95rem', color: 'var(--citruss-text-muted)', lineHeight: '1.5' }}>
        Use the sidebar on the left side to navigate between individual interactive components. Each story contains full live playgrounds, customizable parameters, and instant copyable code templates!
      </p>
    </div>
  </div>

</div>
